# conda create -n cv python=3.10 -y
# conda activate cv
# conda install -c conda-forge numpy matplotlib opencv -y
# conda install -y ipykernel
# python -m ipykernel install --user --name cv --display-name "Python (cv)"
# conda install -y notebook
# jupyter notebook
import cv2
import numpy as np
import matplotlib.pyplot as plt



image_path = "mug.jpg"  # Change path as needed
img = cv2.imread(image_path)

if img is None:
    print("Error: Image not found!")
    exit()



plt.figure(figsize=(5,5))
plt.imshow(img)
plt.title("Original Image")
plt.axis("off")
plt.show()




colors = ('r', 'g', 'b')
plt.figure(figsize=(8,5))
for i, color in enumerate(colors):
    hist = cv2.calcHist([img], [i], None, [256], [0, 256])
    plt.plot(hist, color=color)
    plt.xlim([0, 256])

plt.title("Color Histogram (Feature Representation)")
plt.xlabel("Pixel Intensity")
plt.ylabel("Frequency")
plt.show()




mean_colors = cv2.mean(img)[:3]
print(f"Mean R: {mean_colors[0]:.2f}, G: {mean_colors[1]:.2f}, B: {mean_colors[2]:.2f}")




